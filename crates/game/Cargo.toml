[package]
name    = "game"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "rts"
path = "src/main.rs"

[dependencies]
engine  = { path = "../engine" }
glam    = { workspace = true }
hecs    = "0.10"
log     = { workspace = true }
mlua    = { version = "0.10", features = ["lua54", "vendored"] }
